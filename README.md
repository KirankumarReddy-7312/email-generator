# Email ID Generator (Python)

A simple Python program that generates an email ID using the user’s first name, last name, and selected email domain.

## Features
- Takes first name and last name as input
- Converts input to lowercase and removes extra spaces
- Allows domain selection (Gmail, Outlook, Yahoo, ProtonMail)
- Generates a clean email ID
- Uses a set to store existing email IDs

## Concepts Used
- input() function
- String methods: lower(), strip()
- Sets
- Conditional statements
- String concatenation

## Technologies
- Python 3


## Project Structure
